{
  "name": "palindrome",
  "version": "1.0.0",
  "dependencies": {
    
  },
  "scripts": {
    "start": "concurrently \"cd server && npm install && npm run watch\" \"cd client && npm install && npm start\"",
    "test": "cd server && npm install && npm test"
  }
}
